public class Fichero-1{
//atributos, constructor y métodos
        int cordx;
        int cordy;

        public puntos(int ncordx,int ncordy){
            cordx = ncordx;
            cordy = ncordy;

        float Calcdistancia(){
            double distancia;
            distancia = math.sqrt(cordx*cordx+cordy*cordy);
            return();
        }
    }
}
